/*

#ifndef BULLET_H
#define BULLET_H

#include <QGraphicsPixmapItem>
#include <QObject>
#include <QGraphicsItem>
#include <QGraphicsRectItem>

class Bullet : public QObject, public QGraphicsPixmapItem, public QGraphicsRectItem
{
    Q_OBJECT
public:
    Bullet();
    Bullet(QGraphicsItem* parent = 0);
public slots:
    void move();
private:
};

#endif // BULLET_H
*/
